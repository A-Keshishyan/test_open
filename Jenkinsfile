pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'building the app ...'
            }
        }
        stage('build 2') {
            steps {
                git 'https://github.com/A-Keshishyan/test_open.git'
            }
        }
    }
}
